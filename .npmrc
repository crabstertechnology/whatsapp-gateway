# Ensure devDependencies are installed even when NODE_ENV=production
# (Railway sets this in build phase, which would otherwise skip them)
production=false
include=dev
fund=false
audit=false
loglevel=error
